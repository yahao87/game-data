# 원장·이력은 append-only NDJSON이다. 두 커밋이 서로 다른 경기를 덧붙였을 때
# 올바른 병합 결과는 "양쪽 줄 모두 유지"이므로 union 병합을 쓴다.
# (기본 병합 전략은 같은 파일 끝에 붙은 줄을 충돌로 처리해 배치를 실패시킨다.)
*.ndjson merge=union

# 파생 산출물(JSON 스냅샷)은 줄 단위 병합이 의미가 없다 — 충돌 시 워크플로가
# 들어온 스냅샷을 채택하고 재집계해서 일관성을 되찾는다.
*.json -merge
